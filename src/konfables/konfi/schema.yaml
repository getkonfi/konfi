app: konfigurator
format: yaml
docs_url: https://github.com/eminert/konfi/blob/main/README.md
hints:
  - konfigurator's own settings
  - theme changes apply immediately
sections:
  - name: Appearance
    key: ""
    fields:
      - key: theme
        label: Theme
        type: enum
        default: catppuccin
        description: color palette for the TUI
        options:
          - gh-dash
          - catppuccin
          - tokyonight
          - gruvbox
          - nord
      - key: nerd_font
        label: Nerd Font
        type: bool
        default: "true"
        description: use Nerd Font glyphs for icons (disable for plain ASCII fallback)
  - name: Navigation
    key: ""
    fields:
      - key: browse_loads_app
        label: Browse Loads App
        type: bool
        default: "false"
        description: load app content while browsing the sidebar instead of requiring Enter
  - name: Logging
    key: ""
    fields:
      - key: log_level
        label: Log Level
        type: enum
        default: info
        description: minimum log level written to the log file
        options:
          - trace
          - debug
          - info
          - warn
          - error
